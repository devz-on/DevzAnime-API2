/**
 * Map DesiDub keys (slug/title normalized) -> canonical DAniApi id.
 *
 * Key examples:
 * - "the daily life of the immortal king 4th season"
 * - "xian wang de richang shenghuo 4"
 */
const desiDubMapOverrides = {
  // "example slug or title": "canonical-daniapi-id",
};

export default desiDubMapOverrides;
